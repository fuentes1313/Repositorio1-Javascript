function yell (str) {
    // declare the variable
    let varUpperCase = "The string in UpperCase is: " + str.toUpperCase ();
    return varUpperCase;
}

console.log (yell ("I want to go to the store"));
console.log (yell ("Time to program"));